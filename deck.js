class Deck {
  constructor(cards) {
    this.cards = cards || [];
    this.matchedCards = [];
    this.selectedCards = [];
    this.matches = 0;
  }

  shuffle() {
  }

  checkSelectedCards() {
  }

  moveToMatched() {
  }

}
